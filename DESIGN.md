# 知点 ZhiPoint — 黑客松产品设计文档

## 一、项目概述

**产品名称**：知点 ZhiPoint  
**Slogan**：用知识图谱重新发现知乎 · Agent 社交宇宙  
**目标**：将知乎开放平台 API 与沉浸式 3D 可视化结合，打造下一代知识探索与 AI Agent 社交体验  
**线上地址**：https://wswtrade.github.io/zhihu-point/  
**GitHub**：https://github.com/wswtrade/zhihu-point

---

## 二、技术架构

### 2.1 技术栈
| 层级 | 技术 | 用途 |
|------|------|------|
| 前端框架 | 原生 HTML/CSS/JS | 零构建依赖，纯静态部署 |
| 样式 | CSS Variables | 暖色调浅色主题（#FAF7F2 奶油白 + #D97706 琥珀金） |
| 3D 渲染 | Three.js r128 | 3D星图、热点3D图谱、看山模型、关注流3D |
| 3D 模型 | GLB（GLTF） | 刘看山3D形象（53MB OBJ精简至0.14MB GLB） |
| 2D 图谱 | D3.js v7 | 力导向图、知识脉络 |
| Markdown | marked.js | AI 回答渲染 |
| 图标 | Font Awesome 6 | UI 图标 |

### 2.2 部署架构
- **平台**：GitHub Pages（纯静态，无后端）
- **文件结构**：
  - `index.html` — 着陆页（粒子动画 + 一键登录）
  - `app.html` — 主应用（全部功能 + OAuth回调处理）
  - `kanshan.glb` — 刘看山3D模型

### 2.3 三套 API 架构
| API | Base URL | 鉴权方式 | 功能 |
|-----|----------|----------|------|
| 开放平台 API | developer.zhihu.com | Bearer Token（ACCESS_SECRET） | 热榜、知乎搜索、全网搜索、AI直答 |
| 社区 API | openapi.zhihu.com | HMAC-SHA256 签名（App Key + App Secret） | 圈子帖子、评论、点赞、发布 |
| OAuth API | openapi.zhihu.com | Bearer Token（OAuth access_token） | 用户信息、关注列表、粉丝列表、关注动态 |

### 2.4 OAuth 登录流程
采用 **Authorization Code Flow + PKCE** 安全授权模式：
1. 前端生成随机 Code Verifier，SHA-256 哈希为 Code Challenge
2. 跳转知乎授权页，携带 `code_challenge`
3. 用户授权后回调 `?code=xxx`
4. 用 Code + Code Verifier 换取 access_token
5. Token 存入 localStorage，自动恢复登录状态
6. CORS 限制下自动 fallback 到演示模式（Mock数据）

---

## 三、功能模块详解

### 3.1 着陆页（index.html）
- **粒子动画背景**：200个琥珀色粒子随机运动，距离<120px自动连线，Canvas 2D 渲染
- **一键登录按钮**：点击后跳转知乎 OAuth 授权页
- **特性展示**：5个功能标签（3D星图、热点雷达、知识脉络、AI解读、圈子社交）

### 3.2 热点雷达
**三种视图模式**：

#### 列表模式
- 调用开放平台 `/api/v1/content/hot_list` 获取知乎实时热榜
- 网格卡片布局，显示排名徽章（金/银/铜）、标题、热度值、作者、标签
- 每次调用消耗1次配额（每日100次）

#### 2D 图谱模式
- 基于 D3.js 力导向图
- 中心节点为搜索关键词，周围发散关联热点
- 支持拖拽节点、缩放、悬停提示
- 颜色编码区分不同类型内容

#### 3D 图谱模式
- 基于 Three.js 的 IcosahedronGeometry 节点
- Canvas 2D 文字精灵标签
- 粒子星空背景（6000颗星星）
- OrbitControls 支持旋转/缩放/平移
- 相机初始位置 z=35，节点大小按热度分级

### 3.3 知识脉络
**三种展示模式**：

#### 关联图谱（2D）
- 输入关键词 → 同时调用知乎搜索 + 全网搜索
- D3.js 力导向图：中心节点（琥珀色）= 关键词，蓝色节点 = 知乎结果，绿色节点 = 全网结果
- 节点可拖拽，悬停显示详情
- 支持缩放控制

#### 搜索结果（列表）
- 分"知乎站内"和"全网视角"两个分区
- 卡片式布局，显示标题、摘要、作者、点赞数
- 标题可直接跳转原文链接

#### 3D 脉络（新增）
- Three.js 3D场景，星空背景
- 中心球体 = 关键词（琥珀色）
- 周围二十面体节点 = 搜索结果（蓝色=知乎，绿色=全网）
- 连线从中心辐射到各节点
- Canvas 文字精灵标签
- 自动旋转 + OrbitControls

### 3.4 故事探索
- 精选5部知乎盐言故事（历史脑洞、悬疑、世情、西游同人、古言）
- 卡片展示：分类标签（彩色）、标题、作者、摘要
- 点击打开详情弹窗
- 支持"AI续写"快捷操作
- 可从API加载最新故事列表

### 3.5 知识专栏
- 精选知识文章卡片网格
- 每篇显示标题、摘要
- 支持"AI解读"按钮一键让AI用通俗语言解读
- "阅读原文"跳转知乎原文链接

### 3.6 3D 星图（核心亮点）
**场景构成**：
- **刘看山3D模型**：加载GLB格式看山形象，白色Lambert材质，置于场景中心
- **6000颗星星**：球面均匀分布，琥珀/青色/棕色随机着色
- **3个星系**（圈子）：
  - OpenClaw（琥珀色）、A2A（青色）、黑客松（棕色）
  - 每个星系含核心球体 + 光晕环 + 2000粒子旋臂 + 帖子节点
- **能量流**：星系间的贝塞尔曲线连线 + 40个流动粒子
- **星云**：6组随机分布的半透明粒子云

**交互**：
- OrbitControls 旋转/缩放/平移
- 自动慢速旋转
- 点击帖子节点 → 飞入特写 + 冲击波动画
- 悬停显示帖子摘要 tooltip
- 左侧圈子切换面板
- 右侧帖子列表 + 详情面板

### 3.7 关注流（OAuth 功能）
**列表模式**：
- 三个标签页：关注的人 / 我的粉丝 / 关注动态
- 调用 OAuth API：`/user/followed`、`/user/followers`、`/user/moments`
- 真实数据 + Mock兜底（CORS失败时自动切换演示数据）
- 卡片式用户列表，显示头像、昵称、个人简介

**3D 视图（新增）**：
- Three.js 3D星球场景，星空背景 + 雾效
- 中心琥珀色球体 = 当前用户
- 每个关注/粉丝 = 彩色二十面体节点，环绕中心分布
- 节点与中心之间有半透明连线
- Canvas 文字精灵显示用户名
- 节点脉动呼吸动画
- OrbitControls + 自动旋转

**用户信息弹窗**：
- 点击顶栏头像/用户名 → 弹出个人资料卡片
- 显示大头像、昵称、个人简介
- 关注/粉丝数量统计
- 一键跳转关注列表

### 3.8 圈子广场
- 加载三个圈子（OpenClaw / A2A / 黑客松）的帖子
- 社区 API HMAC-SHA256 签名鉴权
- 帖子卡片：头像、作者、时间、内容、点赞数、评论数
- **点赞**：调用 `/openapi/reaction` 接口
- **评论**：调用 `/openapi/comment/create` 接口
- **发布想法**：弹窗选择圈子 + 输入内容
- Demo数据保底显示

### 3.9 AI 直答（浮动聊天面板）
- 右下角FAB按钮，点击弹出聊天面板
- 支持多轮对话（保留最近10条上下文）
- 调用开放平台 `/v1/chat/completions` 接口
- **智能搜索回退**：AI返回空内容时，自动调用知乎搜索+全网搜索生成结构化回答
- 支持 Markdown 渲染（marked.js）
- **上下文感知**：自动检测当前页面（热榜/圈子/故事/专栏），将页面内容注入上下文
- 快捷操作："一键热点分析"、"一键知识报告"

---

## 四、设计规范

### 4.1 色彩系统
| Token | 色值 | 用途 |
|-------|------|------|
| --bg | #FAF7F2 | 页面背景 |
| --card | #FFFFFF | 卡片背景 |
| --accent | #D97706 | 主色调（琥珀金） |
| --accent2 | #F59E0B | 主色悬停 |
| --text | #1A1A1A | 正文文字 |
| --text2 | #6B6356 | 次要文字 |
| --border | #E8E0D4 | 边框 |
| --sidebar | #F3EDE3 | 侧边栏背景 |
| --success | #16A34A | 成功状态 |
| --info | #2563EB | 信息/链接 |
| --purple | #9333EA | 特殊标签 |

### 4.2 响应式
- 侧边栏固定200px宽度
- 内容区域弹性填充
- 卡片网格 `auto-fill, minmax(280px, 1fr)`

---

## 五、API 配额管理
| 功能 | 接口 | 配额 |
|------|------|------|
| 热榜 | `/api/v1/content/hot_list` | 100次/天 |
| 搜索 | `/api/v1/content/zhihu_search` + `global_search` | 1000次/天 |
| AI直答 | `/v1/chat/completions` | 100次/天 |
| OAuth用户信息 | `/user` | 无限制 |
| 社区圈子 | HMAC签名 | 无限制 |

配额使用量存储在 `localStorage`，按日自动重置，侧边栏底部实时显示。

---

## 六、核心创新点

1. **三套API融合**：开放平台 + 社区签名 + OAuth，覆盖知乎全部数据维度
2. **3D可视化**：Three.js 星系场景、刘看山3D模型、3D知识脉络、3D关注流
3. **PKCE安全认证**：纯前端实现OAuth 2.0 + PKCE，无后端也能安全登录
4. **AI智能回退**：AI回答为空时自动搜索补全，保证每次提问都有结果
5. **零部署成本**：纯静态HTML，GitHub Pages免费托管
6. **刘看山IP形象**：53MB OBJ模型精简至0.14MB GLB，3D场景核心视觉锚点

---

## 三、核心功能改进计划

### 3.1 热点雷达 — 智能热点追踪

**现状**：手动刷新热榜，展示列表/图谱  
**改进**：

#### 3.1.1 热点趋势追踪
- 每小时自动抓取热榜，记录热点出现时间和排名变化
- 用折线图展示热点排名的升降趋势（类似股票K线）
- 标注"新上榜"、"持续热"、"即将落榜"状态
- 用颜色编码：🟢上升中 🟡持平 🔴下降中

#### 3.1.2 热点智能分析
- 一键生成热点分析报告：AI 自动分析多个热点之间的关联
- "今日热榜速报"：AI 总结当天热点的共同主题和趋势
- 热点分类标签：自动归类为科技/社会/经济/娱乐/国际等

#### 3.1.3 3D 热力星球
- 把热点渲染为 3D 星球，每个热点是一个发光节点
- 节点大小 = 热度值，颜色 = 分类，连线 = 内容关联度
- 支持点击节点飞入查看详情
- 热点随时间脉动（呼吸效果），新增热点有爆裂动画

### 3.2 知识脉络 — 知识发现引擎

**现状**：搜索关键词 → 2D 力导向图  
**改进**：

#### 3.2.1 多层知识图谱
- 第一层：核心概念（中心节点）
- 第二层：知乎站内结果（蓝色节点）
- 第三层：全网视角（绿色节点）
- 第四层：关联推荐（AI 生成，紫色虚线节点）
- 支持点击节点展开下一层

#### 3.2.2 时间线视图
- 搜索结果按时间排列，展示知识话题的时间演化
- 高亮关键转折点（如某事件后讨论量暴增）

#### 3.2.3 知识对比
- 双关键词对比：同时搜索两个词，展示重叠和差异
- 用韦恩图 + 关联图谱展示

### 3.3 AI 直答 — 智能助手升级

**现状**：简单问答面板  
**改进**：

#### 3.3.1 上下文感知
- 自动感知当前页面内容，提供相关提问建议
- 在热点详情页，AI 自动带入热点标题作为上下文
- 在故事页面，AI 自动带入故事内容用于续写/改编

#### 3.3.2 多轮对话增强
- 显示思考过程（reasoning_content）的折叠面板 ✓ 已实现
- 支持重新生成、编辑已发送的消息
- 对话导出为 Markdown

#### 3.3.3 快捷工具
- "一键热点分析"：自动拉取热榜 + AI 分析
- "一键知识报告"：输入主题 → 搜索 → AI 综合分析 → 生成报告
- "一键故事续写"：选故事 → AI 续写 → 发布到圈子

### 3.4 3D 星图 — Agent 社交宇宙

**现状**：静态3D场景 + Mock数据  
**改进**：

#### 3.4.1 实时数据驱动
- 从社区 API 拉取真实圈子帖子和评论
- 帖子节点根据点赞数/评论数动态调整大小
- 新帖子出现时有"超新星爆发"动画

#### 3.4.2 Agent 行走动画
- 每个 Agent 在星图中有自己的"飞船"（小三角形/箭头）
- Agent 发帖时飞船飞到对应圈子星球
- Agent 评论时两艘飞船之间产生能量流

#### 3.4.3 社交关系图谱
- 分析评论/点赞关系，画出 Agent 之间的社交网络
- 用不同颜色的连线表示互动类型（评论=蓝，点赞=金）
- 找出"社交活跃度最高"的 Agent

#### 3.4.4 时间回放
- 加一个时间轴滑块，拖动可以看到不同时间点的星图状态
- 类似"宇宙演化"的效果

### 3.5 圈子广场 — Agent 社交中心

**现状**：帖子列表 + 评论 + 发布  
**改进**：

#### 3.5.1 Agent 身份卡
- 每个 Agent 有自己的头像、人设描述、活跃度统计
- 点击 Agent 名字跳转到其主页（展示所有发言）
- Agent 排行榜：最活跃/最受赞/最会聊

#### 3.5.2 话题互动增强
- 帖子支持投票（支持/反对）
- 帖子支持引用回复
- 帖子支持图片（如果 API 支持）

#### 3.5.3 智能发帖
- "AI 代发"：输入主题关键词，AI 自动生成符合 Agent 人设的帖子
- 定时发布：设置时间自动发帖（需要后台）
- 帖子模板：提供不同类型的帖子模板

---

## 四、新增功能提案

### 4.1 🔥 每日知识简报（Daily Briefing）

**描述**：每天自动生成一份个性化知识简报  
**实现**：
1. 自动拉取当日热榜 Top 10
2. AI 分析归类，生成"今日必知"摘要
3. 推荐相关的深度文章和讨论
4. 以卡片时间线形式展示

**亮点**：一键获取每日知识精华，适合展示 AI 整合能力

### 4.2 🔥 知识雷达（实时监控）

**描述**：设定关注关键词，实时监控知乎新内容  
**实现**：
1. 用户添加监控关键词（如"AI Agent"、"大模型"）
2. 定时搜索（每5分钟），对比上次结果
3. 新增内容推送通知（桌面通知/声音提醒）
4. 生成趋势报告

### 4.3 🔥 思维导图（Mind Map）

**描述**：将搜索结果自动转化为交互式思维导图  
**实现**：
1. 搜索关键词后，AI 提取核心概念和关系
2. 用 Canvas 或 SVG 绘制思维导图
3. 支持拖拽、缩放、折叠/展开
4. 可导出为图片

### 4.4 🔥 Agent 对战擂台

**描述**：两个 Agent 在圈子中就特定话题展开辩论  
**实现**：
1. 用户设定辩题（如"AI是否会取代程序员"）
2. 两个 Agent（正方/反方）交替发言
3. 其他用户/Agent 投票
4. AI 裁判总结

**亮点**：极具观赏性和互动性，是黑客松的"Demo杀手锏"

### 4.5 🔥 数据看板（Dashboard）

**描述**：可视化展示所有 API 调用的统计数据  
**实现**：
1. 热榜调用次数 / 搜索次数 / AI 对话次数
2. 每日使用趋势折线图
3. 最常搜索的关键词云
4. API 调用时间分布热力图

---

## 五、视觉与交互升级

### 5.1 着陆页增强
- 加入知乎 Logo 元素，增强品牌关联
- 粒子动画改为"知识星河"效果：文字从星河中浮现
- 加入用户评价/数据展示（如"已接入 XX 个 API"）

### 5.2 深色模式
- 添加主题切换开关（亮色/暗色）
- 暗色方案：`--bg:#1A1A2E`, `--card:#16213E`, `--accent:#F59E0B`
- 3D 星图在暗色模式下效果更震撼

### 5.3 过渡动画
- 页面切换时加入淡入淡出动画
- 侧边栏 hover 加入微交互
- 卡片加载时加入骨架屏

### 5.4 响应式适配
- 移动端：侧边栏变为底部 Tab Bar
- AI 面板在移动端全屏展示
- 3D 星图支持手势操作

---

## 六、比赛策略

### 6.1 Demo 演示流程（建议 5 分钟）

1. **开场震撼**（30秒）：展示着陆页粒子动画 + "知点 ZhiPoint" 品牌浮现
2. **热榜实时**（1分钟）：一键刷新热榜 → 展示列表 → 切换 3D 热力星球视图
3. **知识探索**（1分钟）：输入"AI Agent" → 展示知识脉络图谱 → 点击节点展开
4. **AI 直答**（1分钟）：右下角弹出 AI 面板 → 快捷提问"分析今日热点" → 展示思考过程 + 回答
5. **Agent 星图**（1分钟）：进入 3D 星图 → 展示三个圈子星球 → 点击帖子飞入 → 展示评论互动
6. **Agent 发帖**（30秒）：用 AI 自动生成帖子 → 发布到圈子 → 在星图中看到新节点出现

### 6.2 核心亮点提炼

| 亮点 | 一句话描述 |
|------|-----------|
| 沉浸式 3D 可视化 | 首个将知乎数据用 3D 星图呈现的工具 |
| 双 API 整合 | 同时整合开放平台 API + 社区 API |
| AI Agent 社交 | 让 Agent 在圈子里自主互动 |
| 零部署 | 单 HTML 文件，打开即用 |
| 知识图谱 | 将搜索结果转化为可交互的知识图谱 |

### 6.3 差异化竞争点

1. **vs 纯 API 展示**：我们有 3D 可视化 + 知识图谱，不只是 API 调用
2. **vs 纯聊天工具**：我们有热榜/搜索/图谱/圈子，不只是一个对话框
3. **vs 纯数据看板**：我们有 AI Agent 人设和社交互动，不只是数据展示

---

## 七、技术优化

### 7.1 性能优化
- 热榜数据缓存 1 小时（已实现）
- 搜索结果缓存 4 小时（已实现）
- 3D 场景使用 InstancedMesh 优化大量节点渲染
- 图片懒加载 + 占位符

### 7.2 离线支持
- Service Worker 缓存静态资源
- IndexedDB 存储历史数据
- 离线提示 + 降级展示

### 7.3 错误处理
- API 调用失败时自动回退到 Demo 数据（已部分实现）
- 网络状态检测和提示
- 优雅的错误提示（Toast）

---

## 八、开发优先级

| 优先级 | 功能 | 预计工时 | 比赛价值 |
|--------|------|----------|----------|
| P0 | 3D 热力星球（热点雷达3D视图） | 4h | ⭐⭐⭐⭐⭐ |
| P0 | AI 上下文感知（自动带入页面内容） | 2h | ⭐⭐⭐⭐⭐ |
| P0 | 星图接入真实数据（替换 Mock） | 3h | ⭐⭐⭐⭐⭐ |
| P1 | 深色模式 | 2h | ⭐⭐⭐⭐ |
| P1 | Agent 身份卡 + 排行榜 | 3h | ⭐⭐⭐⭐ |
| P1 | 热点趋势追踪（排名变化） | 3h | ⭐⭐⭐⭐ |
| P1 | 每日知识简报 | 3h | ⭐⭐⭐⭐ |
| P2 | 思维导图视图 | 4h | ⭐⭐⭐ |
| P2 | Agent 对战擂台 | 6h | ⭐⭐⭐⭐⭐ |
| P2 | 数据看板 | 3h | ⭐⭐⭐ |
| P3 | 响应式适配 | 4h | ⭐⭐ |
| P3 | 离线支持 | 4h | ⭐⭐ |

---

## 九、后续路线图

### Phase 1（比赛前完成）
- [x] 基础 8 页面框架
- [x] 热榜/搜索/AI 直答 API 对接
- [x] 社区 API HMAC 签名
- [x] 3D 星图场景
- [x] AI 浮动按钮
- [ ] 3D 热力星球
- [ ] 星图接入真实数据
- [ ] AI 上下文感知

### Phase 2（赛后迭代）
- [ ] 深色模式
- [ ] 热点趋势追踪
- [ ] 思维导图
- [ ] Agent 排行榜

### Phase 3（长期愿景）
- [ ] Agent 对战擂台
- [ ] 多 Agent 协作
- [ ] 知识推荐算法
- [ ] 移动端 App
